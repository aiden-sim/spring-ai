spring:
  application:
    name: mcp-client
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: gpt-4o
    mcp:
      client:
        sse:
          connections:
            mcp-server:
              url: http://localhost:8081

server:
  port: 8080

